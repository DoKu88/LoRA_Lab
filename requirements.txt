# LoRA_Lab — pinned Blackwell/sm_120 stack (RTX 5090, CUDA 12.8)
# Verified working: torch 2.10.0+cu128, capability (12, 0) = sm_120.
# Install torch family from the cu128 index (see environment.yml / README):
#   pip install --index-url https://download.pytorch.org/whl/cu128 torch==2.10.0 torchvision==0.25.0
# then: pip install -r requirements.txt

# --- core DL stack (CUDA 12.8 / sm_120) ---
torch==2.10.0
torchvision==0.25.0
torchao==0.17.0
triton==3.6.0

# --- HF + PEFT + quantization ---
transformers==5.5.0
peft==0.19.1
bitsandbytes==0.49.2
trl==0.24.0
accelerate==1.14.0
datasets==4.3.0
tokenizers==0.22.2
safetensors==0.8.0
sentencepiece==0.2.1

# --- numerics / io ---
numpy==2.4.4
pandas>=2.2
pyarrow>=17.0          # parquet results dataset
matplotlib>=3.9        # gpu-mem-vs-iteration plot
tabulate>=0.9          # markdown table render (df.to_markdown)
psutil>=5.9            # host-RAM (RSS) probe — Phase 0.5 peak-RAM measurement
tqdm>=4.66             # meta-train progress bar (also a transformers transitive dep)
petname>=2.6           # human-friendly run-name suffix (adjective-noun)
pyyaml>=6.0            # config + tasks manifest
rouge-score>=0.1.2     # ROUGE-L eval metric

# --- experiment tracking (best-effort, offline-capable) ---
wandb>=0.18

# --- dev / test ---
pytest>=8.0
