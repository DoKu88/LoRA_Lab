# Large binary source PDFs — keep out of git history
pdfs/

# Local Claude Code / agent tooling config
.claude/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
build/
dist/

# --- training run outputs (heavy, regenerable) -----------------------------
# Trained weights / checkpoints are NOT committed; configs + results are.
results/runs/
wandb/

# Keep the curated results artifacts under version control:
#   results/comparison.csv / .parquet / .md  (the deliverable table/dataset)
#   results/mem_trace/*.csv                   (per-run memory traces)
#   results/plots/*.png                       (gpu-mem-vs-iteration plots)
# (these are small and are the point of Phase 0, so they are tracked)

# HF / model caches if they ever land in-tree
.cache/
hf_cache/
